
<% if signed_in? %>
  <%= form_for :session, url: logout_path, method: 'delete' do |f| %>
    Signed in as <%= link_to current_user.email, current_user %>
    <%= f.submit 'Logout', id: 'logout' %>
  <% end %>
<% end %>
</div>
